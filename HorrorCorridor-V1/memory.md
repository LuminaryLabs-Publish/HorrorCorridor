# HorrorCorridor Memory

## Purpose

HorrorCorridor-V1 is a multiplayer first-person horror expedition prototype: a finite entrance opens into an effectively endless ruined-building delve where directional monster encounters, scare-earned collection, and room discoveries drive the run. The current priority is cohesive playable depth, natural domain ownership, and human-visible quality.

## Product Direction

- HorrorCorridor is a replayable cooperative first-person horror expedition about getting lost, leaving traces, listening for monsters, deliberately risking their full scares, and coordinating under limited information.
- The product identity is not a generic shooter or lobby demo. It is a dark, readable, minimal-UI dungeon crawl through successive ruined buildings. Encounters survived are the run score; the Monster Index is the collection game.
- Long-term content should deepen natural world branches through building/room identities, monster species and scare rituals, discoveries, boons, props, decals, lighting moods, and hazards.
- Each meaningful change should either improve prototype parity, strengthen the kit/content pipeline, improve the horror corridor atmosphere, or make the runtime easier to validate and extend.
- Product direction details live in `docs/HorrorCorridor-Product-Direction.md`.
- Lasting art-direction decision: avoid default neon-green cyber/cyber-city styling. The target is dirty overgrown broken-city horror: muddy terrain, roots, moss, rubble, broken concrete/brick, rusted practical objects, localized amber light, dim cold-green depth, wet response, and rare sickly encounter pressure.

## Architecture Shape

- App screens and menus stay React-owned.
- The active game loop stays imperative and frame-local for movement, camera, render sync, networking cadence, and minimap drawing.
- Shared network snapshots stay small transport contracts; richer runtime-only state is rebuilt inside the game/runtime layer.
- `src/protokits` is a local capability-kit SDK. Natural domains own coherent state/meaning/reset/snapshot; kits install atomic idempotent behavior through explicit `provides`/`requires`; services expose controlled cross-domain verbs; hosts only adapt platforms.
- HorrorCorridor-specific values live in `src/protokits/presets/horror-corridor-preset.ts` and content packs, not inside generic domain kits.
- Scene richness should be built through compositional local kits: grid fields, CPU raymarch sampling, object placement, procedural PBR-style materials, texture placement, lighting placement, walkthrough validation, and scene-generation manifests.
- The next terrain/world architecture should separate terrain, modular corridor tiles, wall modules, vegetation/grass objects, debris props, decals, and practical lights into generic local domain kits customized by HorrorCorridor presets.
- The first scene-generation render pass is descriptor-first and non-blocking: `worldBuilder.ts` creates a `maze-scene-dressing` group from preset-driven descriptors without adding collision or gameplay interaction.
- Prop/material fidelity is now a local domain capability: `prop-material-fidelity-domain-kit` declares generic mesh-profile, material-profile, and visible-content threshold services, while `sceneDressingRenderer.ts` owns Three.js mesh/material realization from descriptors.
- Geometry-aware placement is now a first-class local kit path: triangle surface sampling, socket graphs, footprint layouts, and scene bundles sit between raw raymarch hits/cells and final prop descriptors.
- HorrorCorridor pins NexusEngine public `main` commit `d41992636de2752f1ad9047b80701e6313f19b87`. The composition installs 36 selected core kits, 73 local descriptor/behavior kits, and 6 executable composition kits; every current natural state owner is mounted with isolated reset and deterministic replay proof.

## Current Conventions

- Start every future HorrorCorridor run from `.agent/`: review `start-here.md`, `workflow.md`, `intention.md`, `live-agent.md`, `memory.md`, `goal.md`, `feedback.md`, and then skim `change-log.md` before broader repo exploration.
- Use `npm run harness:horror-corridor` to refresh repo-owned Codex guidance whenever durable product, workflow, kit, or domain-service-kit rules are introduced through chat.
- `HorrorCorridor-Harness` also owns the Luna swarm orchestrator at `scripts/horror-corridor-swarm.mjs`. Large change batches should use scoped requests, isolated worktrees, disjoint parallel paths, bounded `gpt-5.6-luna` predictions, deterministic validation, and an integration branch; workers never merge or push the default branch.
- Use `npm run visual:match` for reference-vs-current visual acceptance loops. It uses the live-player harness for current screenshots, Codex CLI for semantic scoring, and bounded Codex auto-fix attempts for scores below `90`.
- Visual reference generation should follow the NexusDaydreamer-Codex pattern: Codex/imagegen creates the raster image, then `submit-reference` registers it into the visual-match run folder before it is treated as a valid reference.
- Keep `PLAYING` canvas-first. Persistent directions/status HUD should stay hidden unless debug or settings is explicitly opened.
- Press `Q` to toggle the settings/control overlay while playing.
- Pointer capture is for mouse look only. Keyboard movement works in `PLAYING` without pointer lock.
- Use `window.__HORROR_CORRIDOR_DEBUG__.extractState()` and `?debug=frames` for runtime validation.
- Use Playwright/browser-visible checks for UI and render changes whenever practical.
- The repo’s intended long-duration game validator is a cumulative live agent: it should operate in bounded episodes that review the screen, choose actions, verify outcomes, and only promote lessons into durable memory after repeated evidence. The live loop and the reviewer loop are separate concerns: one produces artifacts, the other inspects them and writes notes.
- The live agent now uses a real sequential Luna judgment chain: one read-only call follows each browser episode, prediction concurrency stays at one, and each later call receives the original goal plus a bounded default window of the last three call outputs and concise reasoning summaries. The live lane defaults to low reasoning, Codex `priority` service tier, and zero artificial inter-episode delay; every episode logs call duration and the time between calls. Hidden chain-of-thought is not requested or stored; malformed judgments and mismatched history fail closed.
- The completed live proof is `docs/live-agent/runs/2026-07-17T18-42-11-807Z`: seven real Luna calls persisted one expedition through intro, directional approach, exact 3,000 ms monster blackout, last chance, jumpscare, and authoritative `caught`. The run stopped on game loss, not a cap; `docs/HorrorCorridor-Live-Luna-Run-2026-07-17.md` records timings and evidence.
- When making a meaningful feature, content, UX, or architecture change, keep the product direction document aligned so future batches compound toward the same game.
- Scene dressing debug proof should use `window.__HORROR_CORRIDOR_DEBUG__.extractState().latestFrame.sceneDressing` for prop/texture/light/walkthrough counts.
- The repo-owned vocabulary is: kits are composable capability packages; domain-service kits are generic domain owners with stable `provides`/`requires`, metadata, resources, systems, and install surfaces; presets and content packs provide HorrorCorridor-specific customization.
- For lighting, camera, props, decals, particles, fog, materials, HUD, or procedural-world work, invoke the local `horror-visual-director` skill as the top-level gate before accepting the pass.
- Visual scene-generation proof must include a player-view screenshot and luminance/readability judgment. Debug counts prove descriptor output only; they do not prove the scene looks good.
- Use `npm run validate:live-player` against a running app as the repo-local NexusSimulator-aligned live-player harness for starting-scene proof. Preferred local flow is `npm run build`, `npm run start -- --hostname 127.0.0.1 --port 3000`, then `npm run validate:live-player`.
- The live-player harness writes `starting-scene.png` before movement and `movement-scene.png` after `W` movement. Use the former for spawn composition/readability and the latter for locomotion regression proof.
- The global validation skills are now hardened to fail closed for dark/horror scene work: if screenshot/player-view proof is blocked, lighting/material/prop/camera fixes remain `Unverified` until visible proof is restored.
- The active room reference is `docs/visual-targets/horror-corridor-expedition-room-v2.png`. The live chamber has a tight enclosed route, crates, work surface, generator, drain, chain, barrels, fence, broken ceiling, overgrowth, wet ground, amber side lights, and cold-green depth. Broken masonry relief is physically realized through 22 deterministic instanced patches and 741 bricks; 4 standing-water patches and 2 wet-ground patches carry 6 footprint-bound broken-light layers. The generator is a proven 27-part / 708-triangle open-frame silhouette. Two loaded industrial racks now total 82 authored parts / 1,360 triangles / 38 stored-object parts and retain a recognizable two-bay shelf rhythm at spawn, approach, and side look. The connected collapsed ceiling has 26 parts / 240 triangles, 8 aperture-radial fracture paths, 10 chipped edge fragments, and 3 small collapse-rooted rubble clusters; natural wet-concrete floor and damp-spalled ceiling surface identity are proven. Wall/prop surface wear, remaining clutter silhouettes, true reflected-environment response, and overall target edge density remain visibly below the reference. Shelving corrosion and wear identity is the next single UX subject.
- Wet-surface behavior belongs to `water-surface-domain-kit`: the preset owns palette, opacity, roughness, ripple, drift, and reflection values; furnished room descriptors own placement; the Three.js adapter realizes and animates generated material textures. The renderer must reuse the same irregular boundary for both water and its light-response layer so highlights cannot float outside the puddle. This stylized response does not consume or justify `n-core-graphics-reflection-kit`; install that core only if a real planar, probe, or screen-space reflection contract is adopted.
- `npm run validate:wet-reflection` is the promotion gate for the wet-response slice. It requires a clean spawn, movement view, and downward/side view, plus movement, look deltas, readable luminance, and zero bottom-left HUD pixels in every artifact.
- Current validation state after the CDP harness pass: `npm run validate:live-player` uses a self-launched headed Chrome CDP profile and passed with a current screenshot, movement proof, zero visible UI text, readable luminance, and scene-dressing counts. Headless Chromium/WebKit/Firefox launch remains unstable, but CDP validation is now the accepted local backend.
- Text-free is not the same as HUD-free. No-HUD validation must include icon/widget checks in addition to visible text checks; the current real-Chrome report passes both independently.
- Procedural-world iteration should support a solo test path that bypasses multiplayer transport; multiplayer remains available, but high-fidelity lighting/shader/content work should be validated in solo first.
- Planned first-person embodiment: add visible hands as bobbing foreground objects tied to movement/carry/interact state.
- Camera feel should include subtle movement-driven camera shake/bob while preserving stable idle and readable mouse look.
- Future procedural objects should be descriptor-driven, non-blocking until intentionally upgraded, alpha-clipped, and procedurally textured so tables, crates, panels, ducts, debris, and other props do not read as clean placeholder boxes.
- Terrain vegetation is descriptor-driven rather than a renderer one-off: `overgrowth-object-kit` owns clipped grass blades, opaque root strips, and hanging-vine realization; room manifests own placement, and floor-bound vegetation samples the local terrain height.
- ProtoKit/world visual effects must be implemented in the Three.js scene/render pipeline, not CSS overlays. Post exposure, bloom, vignette, grading, particles, fog, and shader effects belong to renderer-owned modules so screenshots and gameplay share the same visual truth.
- Keep the repo live-player luminance gate intact. If a darker material direction fails it, fix render/light/material response instead of weakening the harness threshold.
- Random flashlight flicker can darken a proof frame without changing the room. Record and inspect such failures, rerun the same profile to separate stochastic capture timing from persistent regression, and keep the original threshold unchanged.
- Procedural content now follows a smoke-first loop: validate descriptor generation through `/api/procedural-kit-smoke` and `npm run smoke:protokits` before trusting it in the main HorrorCorridor play scene, then verify player-visible output with `npm run validate:live-player:dev`.
- Swarm worktrees share the base checkout's `node_modules` through a read-only symlink. Next dev and build validation in those worktrees must use Webpack because Turbopack rejects dependency symlinks outside its filesystem root; Luna workers leave declared validation commands to the parent orchestrator because their workspace sandbox cannot bind local ports.
- The current graph-backed placement pass improves composition but not final asset fidelity. Future scene work should extend the anchor/socket/layout/bundle graph and replace primitive mesh profiles inside it, rather than adding more count-driven scatter on top.
- Staged procedural loading is a first-class app scene (`LOADING`) before `PLAYING`; current stages are maze field, terrain raycast, object kits, PBR materials, and lighting pass.
- New scene/domain kits should be previewed in isolated testing surfaces before main-scene promotion; use preview proof first, then smoke proof, then live-player proof.
- Object kits should be visually reviewed in human-context browser rooms before promotion. The corridor lamp path now uses `npm run review:object-kit -- corridor-lamp` to capture full review-room screenshots with a generated primary target image, supporting references, current Three.js render, camera/lighting modes, scale context, checklist, notes, raw canvas support captures, and an 8-angle orbit set for silhouette comparison.
- The corridor lamp object kit is composed from scoped local part-profile kits with their own inline configs: foundation, pole, armature, lamp head, cable/conduit, fastener, material, light, and validation. Future complex props should follow this pattern when the reference has distinct visible subsystems.
- Mesh-generating object kits should emit explicit indexed wound triangle descriptors with positions, indices, normals, UVs, material slots, bounds, sockets, and `outward-ccw` winding. The shared owner is `wound-triangle-mesh-domain-kit`; object families should use small inline mesh profiles and pass smoke validation before promotion.
- `wound-triangle-mesh-domain-kit` now owns reusable indexed cylinders along all three axes. `broken-generator-object-kit` composes that neutral primitive with boxes/trapezoids instead of owning renderer geometry; `furnish-chamber-kit` only places and scales the result. The first oversized smooth-disc live iteration was rejected as boiler-like before the retained 1.28-scale spoke version passed the dedicated side-look proof.
- `industrial-shelving-object-kit` now composes a 41-part / 680-triangle two-bay rack from the existing wound boxes, trapezoids, and cylinders. It includes 19 tagged stored-object parts and an explicit muted-olive `painted-utility` material; `furnish-chamber-kit` still only places two wall-side copies. Keep the natural `Shelves` owner and still-open `industrial-shelving-kit` behavior contract separate from this visual descriptor. `npm run validate:industrial-shelving` is the promotion gate.
- Ten mesh-object review rooms exist. The furnished chamber now composes the promoted door, fence, generator, drain, shelving, hanging-chain, barrel, and broken-stair descriptors; concrete barrier and collapsed signpost remain review-only. Use `npm run review:object-kit -- <kit-slug>` before promoting further placements.
- Major world-object families should stay explicit local object kits. `npm run smoke:protokits` is now expected to prove generated prop-kind coverage plus matching material-family and shared triplanar/PBR shader-profile coverage before those kits are trusted in the main corridor.
- Practical corridor landmarks should be prop-plus-light systems when appropriate. Lamps should be explicit object kits whose illumination comes through the shared light descriptor path, not ad hoc mesh-only dressing.
- Public NexusRealtime or ProtoKit capabilities should be adopted when they match the domain cleanly. If they do not, add a new local generic domain-service kit in `src/protokits` and keep HorrorCorridor-specific values in presets/content packs instead of inside the generic kit.
- Domain mapping follows the public NexusEngine ideal architecture: map top-down by asking which natural subjects must exist inside the parent, then recurse only while a child owns independently meaningful truth. Never manufacture depth from algorithms, object parts, renderer work, adapters, categories, or proofs. The canonical active tree contains 358 child domains, 1,085 state truths, 433 kit mounts/428 unique contracts, 59 services, 4 hosts, 6 proofs, and a deepest path of 24 levels. The obsolete ordered-cube Anomaly Rite is retired from this graph; old snapshot fields remain compatibility-only.
- `.agent/domain-service-coverage.md` is generated from the current blueprint plus a passing hash-matched live report. It classifies every natural state owner and every target kit contract without treating state installation as behavior completion; use `npm run domain:coverage` and `npm run domain:coverage:check` to refresh and gate it.
- Current executable proof: 474 installs, 405 registered domain paths, 36 current core kits, 73 local descriptor/behavior kits, 6 composition kits, 359/359 mutable natural owners reset, deterministic post-reset/fresh-runtime replay, and 296 explicitly open behavior contracts. Solo restart closes `resolve-expedition-kit`; Shared Expedition recovery closes six transport/rejoin/snapshot/chronicle contracts; wet concrete closes `settle-slabs-kit` and `crack-slab-kit`; the ceiling fracture/rubble slice closes only `break-ceiling-kit`, `open-ceiling-kit`, `propagate-crack-kit`, and `collapse-masonry-kit`. Do not infer closure for unrelated contract-only natural behavior from state installation or visual descriptor placement.
- `npm run validate:success-path` is the continuous headed-Chrome success gate. It must prove an early studied repel, a later full-scare collection, a claimed offer, score 2, Building 3 entry, matching streamed-room building identity, clean console, and all standard player/runtime gates in one expedition.
- The full furnished chamber is streamed to each new authoritative building origin and aligned to the entry yaw. Keep this one-room-at-a-time boundary so endless visual succession does not multiply the 84-descriptor room and its 741 instanced relief bricks across the whole maze.
- `docs/HorrorCorridor-Natural-Domain-Execution-Reconciliation-2026-07-17.md` is the durable full-objective map. It records natural-name normalization, the explicitly superseded ordered Anomaly branch, 36 reused core kits, 132 closed behavior contracts, 296 open contracts, and current human-view evidence.
- The primary game direction is now an authored entrance followed by an effectively endless ruined-building delve. The score is monster encounters survived; the Monster Index collects creatures only after the player experiences and survives each creature's characteristic scare.
- The first full monster encounter is a spatial-audio stalker: listen for its direction, turn, and hold the flashlight on it to repel it. Close contact forces a three-second blackout and then a final response window under ten seconds; failure ends the run with a jumpscare.
- Gameplay authority belongs outside React/HTML and renderer/audio hosts. Each of the five active top-level domains has a thin composition boundary whose atomic behavior kits reuse the pinned NexusEngine cores; local kits exist only for HorrorCorridor-specific rules, descriptors, and composition.
- Solo attempt construction belongs to Expedition / Departure through `begin-expedition-kit`, implemented by the pure `beginSoloExpedition` operation. The browser shell may supply an opaque attempt ID, clear runtime adapters, and apply the returned bootstrap, but it must not mutate a caught run into a fresh one inside JSX. `npm run validate:restart-after-caught` is the promotion gate for terminal action, loading, new identities, fresh run/Index truth, and resumed movement.
- Shared Expedition / Rejoining owns connection loss, last-known place, recovery request correlation, restored place, and bounded history through the pure operations in `sharedRecovery.ts`. Hosts and transports only present or carry this truth. Recovery UI chooses its lost/recovering phase from `recovery.connection`, not from a lower-level transport status that can race. `npm run validate:reconnect-recovery` is the required two-page promotion gate; it does not claim cross-device PeerJS proof.
- The detailed contract and reuse map lives in `docs/HorrorCorridor-Endless-Expedition-Architecture.md`.
- The operational natural-world and DSK map lives in `docs/HorrorCorridor-Natural-World-Kit-Map.md`. Its lasting no-kit rule is: reuse a consumed core for neutral capability; use presets/content for tuning and placement; use adapters for platform binding; use proofs for evidence; add one local atomic kit only for a missing reusable HorrorCorridor verb or descriptor.
- Wet concrete identity is owned naturally by `Corridor / Ground / Surface / Paving / Concrete / Slabs / Slab`. `concretePaving.ts` publishes `settled-pour-field` slab truth and `settlement-branching` fracture truth; existing terrain-shader and wet-concrete capabilities consume it, and Three.js only realizes it. `npm run validate:floor-material` is the three-view promotion gate.
- Fine ceiling fracture and small settled rubble are owned across existing `Ceiling`, `Ceiling / Openings`, `Decay / Cracking`, and `Ground / Rubble / Fallen Masonry` truth. Existing Nexus world/scene/spatial, object/shape/fidelity, graphics/presentation, wound-mesh, collapsed-ceiling, rubble/debris, furnishing, preset, and host capabilities realize it. No new domain, core, alias kit, or production kit was required. The durable proof is `docs/HorrorCorridor-Ceiling-Fracture-Rubble-Proof-2026-07-17.md`.
- Persistent live-control `turnByRadians` and `lookByRadians` calls must publish their view change into the authoritative player snapshot before the session is held; a local store-only camera change can be overwritten by snapshot replay and is not valid harness proof.
- Ceiling surface condition is one natural state beneath existing `Ceiling`. Preset tuning and existing terrain/object material capabilities translate it through Nexus graphics/presentation into Three.js; do not add a parallel material domain, alias core, or kit for it. `npm run validate:ceiling-material` proves exact descriptor/Nexus agreement, 3 surfaces, 2 generated textures, readable/HUD-free upward views, and reset/replay.
