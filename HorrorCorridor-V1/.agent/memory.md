# Agent Memory

Status: active

## Durable Notes

- `.agent/` is the repo-local control plane. Every future run should start by reviewing `start-here.md`, `workflow.md`, `intention.md`, `live-agent.md`, `memory.md`, `goal.md`, `feedback.md`, and then `change-log.md` before broader repo exploration.
- `HorrorCorridor-Harness` has two repo-owned surfaces: the linear guidance refresh chain and the asynchronous Luna swarm orchestrator. The swarm path pins `gpt-5.6-luna` with medium reasoning, isolates every task in its own worktree/branch, requires declared file scopes and validation, and stops at a human-reviewed integration branch without merging or pushing the default branch.
- `horror-corridor-visual-match` is the repo-owned Codex CLI visual acceptance loop. It should generate or register one reference image, capture current player-view screenshots through the live-player harness, ask Codex to score semantic visual deltas, and auto-fix below-90 results through bounded attempts with artifacts.
- The visual-match CLI follows the NexusDaydreamer-Codex pattern: Codex/imagegen output is not trusted until a submit command registers the image into the run folder, and each attempt owns manifests, prompts, reviews, retry plans, and reports.
- The live agent should be cumulative and episodic rather than a single capped pass: screen review -> action selection -> bounded tool loop -> verification -> review -> lesson candidate -> durable promotion only after repeated evidence. It may run indefinitely under supervision, but the reviewer that reads logs/screenshots and writes notes should stay separate from the live loop itself.
- The live agent's decision point is now a real sequential Luna chain: one read-only judgment follows each live-player episode, later calls receive the original goal plus the last three call outputs and concise reasoning summaries by default, prediction concurrency stays at one, and malformed or history-mismatched judgments fail closed. This live lane defaults to low reasoning, Codex `priority` service tier, no artificial inter-episode delay, and logs call duration plus time between calls. The in-loop judge selects actions; the separate post-run reviewer still owns retrospective analysis.
- The real until-loss proof is `docs/live-agent/runs/2026-07-17T18-42-11-807Z`. Seven Luna calls reused one expedition and ended only when The Still Guest produced the exact three-second blackout, last chance, jumpscare, and authoritative `caught` state. The timing/evidence digest is `docs/HorrorCorridor-Live-Luna-Run-2026-07-17.md`.
- Solo attempts begin through the pure `begin-expedition-kit` operation in `src/features/game-state/domain/beginExpedition.ts`. The React shell supplies only an opaque attempt ID, clears adapters, and applies the returned authoritative bootstrap. `npm run validate:restart-after-caught` must prove caught terminal -> one restart hero action -> visible loading -> new room/player identities -> fresh intro/score/Monster Index -> resumed movement. The passing proof is `docs/live-player-harness/restart-after-caught-proof/`.
- Shared recovery truth belongs to `src/features/networking/domain/sharedRecovery.ts`: preserve the last authoritative place, record disconnect, correlate one reconnect request, accept only host snapshot truth, record restored place/history, then return to play. `GameShell`, PeerJS/BroadcastChannel, React recovery UI, and `GameCanvas` are adapters around that transaction. The Recovery screen must derive its primary lost/recovering phase from `recovery.connection`; lower-level transport status is supporting text and may not prematurely change the phase. `npm run validate:reconnect-recovery` must keep the two-page disconnect/rejoin/resync/move flow passing; cross-device PeerJS recovery remains unverified.
- The current risk is accepting render signal-flow proof as visual-quality proof. Future visual passes must be screenshot-driven and fail closed when the frame is unreadable.
- The local `horror-visual-director` skill is now the top-level gate for HorrorCorridor visual work. Use it before any lighting/material/camera/prop/decal/particle/HUD/procedural-world pass so target-vs-current screenshots drive acceptance.
- Global validation skills now enforce that risk: dark/horror scene work must route through `lighting-balance-scout`, and blocked screenshots mean `Unverified`, not fixed.
- HorrorCorridor-specific tuning belongs in `src/protokits/presets/horror-corridor-preset.ts` and content packs, not inside generic domain kits.
- The current scene-dressing layer is visual-only and non-blocking. Collision or gameplay interaction with props should be added later through explicit occupancy/interaction systems.
- `prop-material-fidelity-domain-kit` is the local domain capability for mesh/material profile intent; `sceneDressingRenderer.ts` is the render owner that turns descriptors into Three.js grouped meshes/materials.
- Human-view proof now routes through `npm run validate:live-player` against a running app, preferably after `npm run build` and `npm run start -- --hostname 127.0.0.1 --port 3000`. The harness also requires the isolated Nexus reset/replay proof, which must leave the live player runtime untouched.
- The live-player harness captures `starting-scene.png` before movement and `movement-scene.png` after movement; use the starting frame for spawn composition and the movement frame for locomotion proof.
- Do not mark room parity visually complete merely because the current report passes. `horror-corridor-expedition-room-v2.png` is the active target; enclosure, route, functional clutter, warm side lights, cold-green depth, wet ground, physical brick relief, bounded broken-light water response, structural ceiling overlap, wet-concrete floor identity, fine aperture-radial fracture, chipped edges, small rubble, damp-spalled ceiling surface identity, and a recognizable loaded industrial storage wall are established, but wall/prop surface wear, true environmental reflection, remaining prop silhouettes, and overall detail fidelity remain below the target.
- No-HUD proof must catch icon-only UI too. The current real-Chrome report has zero visible text and zero bright pixels in the bottom-left HUD crop; keep both gates because either can regress independently.
- Keep the live-player luminance gate intact; do not lower it to accommodate darker material experiments. Tune renderer/light/material response until `npm run validate:live-player` passes.
- Random flashlight flicker can place an otherwise valid movement capture below the support readability metric. Preserve that observation in run logs, rerun to distinguish a stochastic capture from a persistent visual regression, and rely on profile-specific plus human-view evidence; do not weaken the gate or silently relabel the dark frame as passing.
- New procedural content should enter through the smoke-first loop: generate descriptors through `/api/procedural-kit-smoke`, validate with `npm run smoke:protokits`, then wire or tune the same descriptor systems in the main `GameCanvas` path and prove with `npm run validate:live-player:dev`.
- Staged loading is now a real `LOADING` screen before `PLAYING`, currently covering maze field, terrain raycast, object kits, PBR materials, and lighting pass.
- The current scene-generation pipeline now has a geometry-aware intermediate graph: triangle surface anchors -> socket graph -> footprint layouts -> scene bundles. This graph is part of the local ProtoKit/preset architecture and should be extended rather than bypassed when replacing random scatter with more authored placement.
- The target-specific furnished chamber owns composition only; reusable render behavior stays in generic kits. `overgrowth-object-kit` now realizes grass blades, terrain-conformed root strips, and hanging vines, while `furnish-chamber-kit` only places those capabilities around the room target.
- `water-surface-domain-kit` owns generic standing-water material, ripple, and broken-reflection profiles. Puddle and wet-ground descriptors remain terrain-bound, use irregular alpha boundaries, and drift their generated ripple texture in the renderer; room manifests only place the surfaces. The Three.js host now reuses each surface boundary for its full-footprint reflection mask, so highlights cannot detach from the water silhouette. This is a stylized broken-light layer, not planar/probe/SSR reflection, and does not justify installing `n-core-graphics-reflection-kit` yet.
- `npm run validate:wet-reflection` is the bounded wet-surface player-view gate. It captures spawn, post-movement, and downward/side-look views; requires movement, `yawDelta >= 0.5`, `pitchAfter <= -0.18`, readable luminance, and zero bottom-left HUD pixels in every view; and writes `docs/live-player-harness/wet-reflection-proof/report.json`.
- Broken masonry relief is now a proven bounded slice. Existing broken-brick descriptors realize deterministic instanced courses with stagger, omissions, depth jitter, mortar gaps, and restrained tint; the current room reports 22 relief patches and 741 bricks. Human-view parity remains partial because wall/prop materials, reflections, primitive remaining silhouettes, and overall edge density remain below the V2 target.
- New visual/domain kits should get isolated preview/testing surfaces before they are considered ready for the main corridor. Main-scene promotion should follow preview proof, then smoke proof, then live-player proof.
- Object-kit visual review should use human-context review rooms, not raw canvas frames alone. `npm run review:object-kit -- corridor-lamp` is the first implementation: it captures full-page review-room screenshots with a generated primary target image, supporting references, angle, lighting mode, scale context, checklist, notes, raw canvas support artifacts, and an 8-angle orbit comparison before any lamp promotion decision.
- `corridor-lamp-object-kit` is no longer treated as one monolithic mesh/profile. It composes nine scoped part-profile kits with inline configs: foundation, pole, armature, lamp head, cable/conduit, fastener, material, light, and validation.
- `wound-triangle-mesh-domain-kit` is the shared owner for explicit indexed triangle mesh descriptors. New mesh-object kits should expose positions, indices, normals, UVs, material slots, bounds, sockets, and `outward-ccw` winding; smoke validation should catch malformed descriptors before visual review or live promotion.
- The shared wound-mesh seam supports explicit indexed cylinders along `x`, `y`, and `z`; mesh-object kits should reuse that primitive when a round functional silhouette is required instead of building a local renderer shortcut or a new shape domain.
- The broken generator is the first accepted player-view silhouette slice from that seam. `broken-generator-object-kit` now composes 27 parts / 708 triangles into an open frame, flywheel with spokes, tank, engine block, cooling fins, exhaust, controls, socket, and cable. The room only scales and places it through `furnish-chamber-kit`. The first larger smooth-disc iteration was rejected as an oversized boiler; the retained `1.28` room scale passes isolated human inspection and the dedicated headed side-look proof. This closes only the bounded generator silhouette, not general material fidelity or full-room parity.
- Ten mesh-generating object-kit review rooms exist. The target chamber now composes the door, fence, generator, drain, shelving, chain, barrels, and broken stair; barrier and signpost remain unpromoted.
- Major world-object families should stay explicit local object kits rather than anonymous descriptor branches. Smoke validation now checks `sceneObjectKitReview` coverage so generated prop kinds, material families, and shared triplanar/PBR shader profiles stay aligned before main-scene promotion.
- Practical corridor landmarks should be paired prop-plus-light systems when appropriate. Lamps are not mesh-only dressing; they should enter as object kits and emit through the shared scene light descriptor path so placement, smoke validation, and player-view lighting stay consistent.
- The harness-generated guide should define the repo vocabulary explicitly: a kit is a composable capability package, while a domain-service kit is a generic domain owner with stable `provides`/`requires`, metadata, and install surfaces that are customized by HorrorCorridor presets and content packs.
- Follow the public NexusEngine ideal model: the game slice is a composition; domains are natural bounded truth owners; atomic kits install idempotent behavior over explicit core requirements; services expose controlled verbs; hosts adapt platforms; proofs validate. Map top-down by asking which natural subjects must exist inside the parent, and stop when a child would only be an algorithm, object part, category, adapter, or proof. The active tree contains 358 child domains, 1,085 state truths, 433 kit mounts/428 unique contracts, 59 services, 4 hosts, 6 proofs, and a deepest path of 24. The ordered-cube Anomaly Rite is retired from the active graph.
- `.agent/domain-service-coverage.md` is the generated truth surface that prevents installed state owners from being mistaken for implemented behavior. Refresh it with `npm run domain:coverage` after a passing current-tree live report, and gate drift with `npm run domain:coverage:check`.
- Current hash-matched proof mounts 474 kits over 405 registered paths, installs 36 core kits, 73 local descriptor/behavior kits, and 6 composition kits, resets all 359 natural state owners, reproduces deterministic replay, and leaves 296 behavior contracts explicitly open. The room summary reports 77 architectural surfaces, 36 props, 9 mesh objects, 4 standing-water patches, 2 wet-ground patches, 6 boundary-matched broken-reflection layers, 26 collapsed-ceiling parts, 8 aperture-radial fracture paths, 10 edge fragments, and 3 collapse-rooted rubble clusters.
- `npm run validate:success-path` proves one continuous headed-Chrome intro -> early repel/study -> full-scare survival/collection -> claimed offer -> Building 3 path. The detailed report is `docs/live-player-harness/success-path-proof/report.json`; its final room summary must report the same building number as the expedition.
- The furnished chamber is a bounded streamed room provider. On each new authoritative building number it moves to the entry origin and aligns its local route with the entry yaw; this makes endless building succession visible without cloning the 84-descriptor room and its 741 instanced relief bricks across the maze.
- The full objective reconciliation lives in `docs/HorrorCorridor-Natural-Domain-Execution-Reconciliation-2026-07-17.md`; use it to distinguish natural-name normalization, the explicitly retired Anomaly branch, generic owner installation, closed behavior, and the 296 open contracts.
- The durable primary loop is now entrance -> effectively endless ruined-building delve -> monster encounter -> room offer -> next building. Encounters survived are the run score; there is no duration victory.
- Monster collection is scare-earned. Seeing reveals an Index entry, early repulsion teaches it, and surviving the creature's full characteristic scare collects it.
- The first complete monster is a positional-audio stalker. The player listens, turns, and holds the flashlight beam on it. Close contact forces an exact three-second blackout, followed by a final response window shorter than ten seconds; failure produces a jumpscare and authoritative run defeat.
- Random flicker is atmosphere and warning, not an abstract loss meter. Defeat belongs to the monster encounter.
- HorrorCorridor pins NexusEngine public `main` commit `d41992636de2752f1ad9047b80701e6313f19b87` and installs 36 cores, including startup/platform, ledger, skybox, framing/output/UI, debug/capture, and vegetation alongside the earlier runtime owners. Agent, compute, headless editor, MLNN, policy, reflection, speech, and utility cores remain uninstalled until a real player flow consumes them.
- Each of the five active top-level HorrorCorridor domains exposes one thin composition boundary over atomic child behavior kits. React/HTML, `GameShell`, `GameCanvas`, Three.js, Web Audio, PeerJS, and storage remain hosts/adapters and must not decide gameplay outcomes.
- `docs/HorrorCorridor-Endless-Expedition-Architecture.md` is the durable detailed product, natural-domain, core-kit reuse, multi-agent ownership, and thin-host contract.
- `docs/HorrorCorridor-Natural-World-Kit-Map.md` is the operational DSK map. Before adding a local kit: reuse a consumed core when it owns the neutral capability; use presets/content for tuning and placement; use adapters for platform bindings; use proofs for evidence; add one atomic local kit only for a still-missing reusable HorrorCorridor verb or descriptor.
- `wound-triangle-mesh-domain-kit` owns the generic closed convex-slab primitive. `collapsed-ceiling-object-kit` composes 26 parts / 240 outward-CCW triangles, including 8 dark fracture paths and 10 chipped edge fragments; `furnish-chamber-kit` places it plus 3 small existing rubble/debris families around the breach. Natural state publishes localized collapse, opening, cracking, rubble, and fallen-masonry truth.
- Ceiling surface material identity belongs to the existing natural `Ceiling` owner, not a material taxonomy. Its `surface` state publishes damp-spalled reinforced concrete/brick condition, aggregate/seam pattern, roughness, and relief. Existing furnishing, terrain/object material, Nexus graphics/presentation, and Three host paths realize 3 ceiling surfaces and 2 generated textures. `npm run validate:ceiling-material` is the promotion gate. No new domain, core, alias kit, or production kit was required.
- Industrial shelving silhouette identity stays inside the existing `Corridor / Ruin / Remnants / Storage / Shelves` natural boundary and the existing `industrial-shelving-object-kit`. The retained two-bay profile has 41 parts / 680 triangles / 19 stored-object parts; the room places two for 82 / 1,360 / 38. `npm run validate:industrial-shelving` is the headed approach/side-look gate. This visible descriptor does not close the separate natural `industrial-shelving-kit` behavior contract, which remains open until a consuming shelf-membership or occupied-tier flow exists. The next one-feature subject is shelving corrosion and wear identity.
- Scripted `turnByRadians` and `lookByRadians` calls on the persistent live-control surface must commit view angles to the authoritative player snapshot before `hold()`. Store-only camera mutation can be overwritten by snapshot replay and is not valid live proof.
- `npm run validate:ceiling-fracture` is the retained promotion gate for Ceiling/Openings, Decay/Cracking, Ground/Rubble, and Fallen Masonry state, descriptor counts, authoritative movement/look, three HUD-free readable views, and reset/replay. Its durable digest is `docs/HorrorCorridor-Ceiling-Fracture-Rubble-Proof-2026-07-17.md`.
- Wet concrete identity belongs to `Corridor / Ground / Surface / Paving / Concrete / Slabs / Slab`, implemented by pure `concretePaving.ts` state with `settled-pour-field` slab membership/alignment and `settlement-branching` cracks. The existing terrain shader and wet-concrete descriptor capabilities consume that truth; Three.js only realizes it. `npm run validate:floor-material` is the promotion gate, and its three HUD-free/readable views close `settle-slabs-kit` and `crack-slab-kit` without a new domain, core, or alias kit.
- The retained entry framing starts at yaw `-Math.PI / 2 + 0.42` and pitch `-0.18` so the first player view presents the room and concrete route rather than empty darkness. This is a lasting readability decision; camera behavior still recenters through the existing look rules.
