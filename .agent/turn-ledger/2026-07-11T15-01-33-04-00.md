# Turn Ledger: HorrorCorridor Interaction Target Intent Authority

**Timestamp:** `2026-07-11T15-01-33-04-00`

## Plan ledger

**Goal:** record the source-backed interaction-target audit and route the next implementation through explicit cube/slot claims, typed results and visible-frame correlation.

- [x] Compared the full Publish inventory with the central ledger.
- [x] Excluded `TheCavalryOfRome`.
- [x] Selected only `LuminaryLabs-Publish/HorrorCorridor`.
- [x] Traced client action inference and TRY_INTERACT construction.
- [x] Traced host target resolution and interaction mutation.
- [x] Traced snapshot publication and client projection.
- [x] Identified active domains, implemented kits and services.
- [x] Added the timestamped architecture and system audit set.
- [x] Refreshed root `.agent` routing and registry state.
- [x] Pushed directly to `main` without a branch or pull request.
- [ ] Runtime implementation and executable fixtures remain future work.

## Decision

The interaction path requires an explicit target-intent boundary before more multiplayer interaction behavior is added.

```txt
current request:
  actor + action

required request:
  command + actor + room + run + epoch + sequence
  + observed snapshot revision
  + explicit cube/slot target and target revision
```

## Evidence

```txt
protocol supports cubeId and slotId
active caller omits both
pickup resolves nearest cube at host execution
place resolves first empty slot at host execution
remove resolves last occupied slot at host execution
mutation returns GameState rather than a typed result
remote no-op still publishes a snapshot
```

## Result

Documentation now defines:

```txt
explicit target observation
claim admission
non-mutating preflight
atomic cube/slot/ownership/sequence commit
typed result and idempotent retry
result-linked snapshot publication
client acknowledgement
first interaction-frame receipt
```

## Validation boundary

No runtime source, dependency, package script, network behavior, rendering behavior or deployment configuration changed. Existing package commands and browser smokes were not executed in this documentation-only connector pass.
