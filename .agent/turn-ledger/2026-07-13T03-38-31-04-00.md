# Turn Ledger: Client Join Attempt Reconciliation

**Repository:** `LuminaryLabs-Publish/HorrorCorridor`  
**Timestamp:** `2026-07-13T03-38-31-04-00`  
**Branch:** `main`

## Summary

Selected HorrorCorridor because its completed client-join audit was newer than the central ledger. Reconciled the interaction loop, domains, 29 implemented kits, offered services, authority boundary, required fixtures and central-sync state without changing runtime behavior.

## Plan ledger

**Goal:** complete one repo-local and central documentation reconciliation for client join-attempt admission.

- [x] Compare ten Publish repositories.
- [x] Exclude `TheCavalryOfRome`.
- [x] Confirm nine eligible ledgers and root `.agent` entrypoints.
- [x] Select HorrorCorridor as the only unsynchronized eligible repository.
- [x] Inspect join input, provisional store mutation, PeerJS and local-bridge connection paths.
- [x] Preserve the complete kit and service inventory.
- [x] Add timestamped reconciliation audits.
- [x] Refresh root `.agent` routing and machine state.
- [x] Push only to `main`.
- [ ] Implement the authority and run executable fixtures.

## Main finding

```txt
raw client join intent
  -> provisional room and roster commit
  -> accepted-looking lobby projection
  -> networking readiness true
  -> transport connection begins
  -> no host acknowledgement or typed terminal join result
```

The local bridge additionally publishes `connection-open` and `connected` immediately after a one-way `client-connect` post.

## Required authority

```txt
corridor-client-join-attempt-admission-authority-domain
```

## Validation

Documentation only. No runtime, network, gameplay, rendering, package, dependency or deployment behavior changed. No branch or pull request was created.