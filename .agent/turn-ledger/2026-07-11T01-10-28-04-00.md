# HorrorCorridor Turn Ledger

**Timestamp:** `2026-07-11T01-10-28-04-00`

## Goal

Reconcile the newer repo-local audit with the central ledger and refine the run-exit/session-epoch boundary without changing runtime behavior.

## Work completed

```txt
selected HorrorCorridor after full org/ledger comparison
confirmed TheCavalryOfRome exclusion
confirmed central ledger lagged the repo-local run-exit audit
reviewed GameShell return, start, transport, and message projection paths
reviewed RoomState, ReplicatedGameSnapshot, and NetworkEnvelope identity fields
refreshed START_HERE, current-audit, next-steps, known-gaps, validation, and kit-registry
added architecture, render, gameplay, interaction, session-authority, and deploy audits
updated central repo ledger and internal change log
```

## Finding

`returnToLobby()` performs a local projection change, while the room, snapshot, transport, and shell-level callback admission remain live. Because active protocol messages carry no complete run identity or monotonic session epoch, stale messages can cross the exit/re-entry boundary.

## Decision

Do not prioritize renderer extraction, visual polish, maze content, cadence tuning, or pause-menu redesign. Implement run identity, typed exit authority, lifecycle publication, callback-generation admission, and deterministic fixtures first.

## Validation state

```txt
runtime changed: no
branch created: no
pull request created: no
existing commands run: no
missing fixtures documented: yes
pushed to main: yes
```
