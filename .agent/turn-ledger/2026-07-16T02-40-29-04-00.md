# HorrorCorridor Turn Ledger

**Timestamp:** `2026-07-16T02-40-29-04-00`  
**Repository:** `LuminaryLabs-Publish/HorrorCorridor`  
**Branch:** `main`  
**Status:** `ooze-rng-stream-replay-authority-audited`

## Summary

Selected HorrorCorridor after the complete Publish inventory and central-ledger comparison found no new, missing, root-agent-missing, undocumented or runtime-ahead eligible repository. `TheCavalryOfRome` remained excluded. HorrorCorridor had the oldest synchronized timestamp, followed by TheOpenAbove.

The audit found that the maze seed is explicit, but host-authoritative ooze decisions use ambient `Math.random()` because `GameCanvas` does not pass the optional RNG accepted by `advanceOozeTrail()`. The concrete trail replicates to clients, but the algorithm, stream, cursor and draw history do not.

## Plan ledger

**Goal:** document one replayable ooze RNG authority without modifying runtime behavior.

- [x] Compare 11 Publish repositories and ten eligible ledgers.
- [x] Confirm ten root `.agent` states and synchronized heads.
- [x] Select only HorrorCorridor.
- [x] Inspect `GameCanvas.tsx`, `oozeRules.ts`, `gameTypes.ts`, `shared.ts`, package scripts and existing audit state.
- [x] Identify the interaction loop, domains, 29 kits, two adapters and offered services.
- [x] Add the new tracker and audit family.
- [x] Refresh root `.agent` routing and machine inventory.
- [x] Reconcile `LuminaryLabs-Dev/LuminaryLabs` on `main`.
- [x] Create no branch or pull request.
- [ ] Implement and execute deterministic replay fixtures.

## Finding

```txt
GameCanvas host cadence
  -> advanceOozeTrail({ nowMs, playerPositions })
  -> rng omitted
  -> resolveRng uses Math.random
  -> decay and visual parameters consume ambient draws
  -> snapshot publishes concrete ooze values only
```

## Required authority

```txt
corridor-ooze-rng-stream-replay-authority-domain
```

## Validation

Documentation only. No runtime command, build, browser proof or deployment smoke was executed.