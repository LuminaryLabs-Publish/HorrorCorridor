# Turn Ledger: HorrorCorridor Data-Channel Roster Admission

**Timestamp:** `2026-07-12T14-22-01-04-00`  
**Repository:** `LuminaryLabs-Publish/HorrorCorridor`  
**Branch:** `main`

## Summary

Selected HorrorCorridor after comparing all ten accessible Publish repositories, excluding TheCavalryOfRome and confirming the remaining nine had central-ledger and root `.agent` coverage. The source audit found that the PeerJS host emits `peer/connection-open` immediately after candidate creation, before `DataConnection.open` is proven, and `GameShell` uses that event to admit a connected lobby member.

## Plan ledger

**Goal:** document one authoritative boundary between a transport connection candidate and an admitted lobby member.

- [x] Compare current organization inventory and ledger timestamps.
- [x] Verify eligible root `.agent` entrypoints.
- [x] Select only HorrorCorridor as the oldest eligible repository.
- [x] Inspect PeerJS connection construction, events, session mutations and lobby controls.
- [x] Preserve the 29-kit inventory and full domain map.
- [x] Add the timestamped audit family.
- [x] Refresh root `.agent` routing and registry.
- [x] Synchronize the central ledger and change log.
- [x] Push only to `main` and create no branch or PR.
- [ ] Implement and execute admission fixtures later.

## Finding

```txt
PeerJS connection callback
  -> candidate inserted into map
  -> handlers installed
  -> peer/connection-open emitted unconditionally
  -> GameShell admits connected guest
  -> room roster and visible player count advance
  -> lobby broadcast may send to zero because channel is not open
  -> actual open callback is suppressed by one-shot guard
```

A never-open or error-only connection can remain represented as a connected lobby member. Host start can then bootstrap and render a player who never received START_GAME or SYNC.

## Required authority

```txt
corridor-data-channel-roster-admission-authority-domain
```

The authority must require actual channel-open evidence, bind connection identity to actor identity, atomically commit a monotonic roster revision, seal start eligibility and publish first-visible-lobby-frame proof.

## Validation

Documentation only. No runtime, network, gameplay, render, dependency, script or deployment changes were made. No browser or PeerJS fixture was run.