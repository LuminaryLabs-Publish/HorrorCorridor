# Turn Ledger — 2026-07-17T20-41-29-04-00

## Repository

`LuminaryLabs-Publish/HorrorCorridor` on `main`.

## Selection

The full Publish inventory contained 11 repositories. Ten were eligible after excluding `TheCavalryOfRome`. All eligible repositories had central ledgers, root `.agent` state and synchronized documented heads. HorrorCorridor had the oldest eligible central timestamp and was the only selected repository.

## Work completed

- Reconfirmed the application, session, transport, protocol, gameplay, rendering, debug and deployment domains.
- Preserved the complete 29-kit and two-adapter service inventory.
- Audited real PeerJS host DataConnection open admission.
- Confirmed `createHost()` invokes `emitConnectionOpen()` unconditionally after attaching its listener.
- Confirmed the one-shot guard prevents the later actual `open` event from correcting premature admission.
- Confirmed `GameShell` converts the event into roster membership and a `player-joined` broadcast.
- Added architecture, render, gameplay, interaction, transport-connection, deploy and central-sync audits.
- Refreshed required root `.agent` documents and the machine registry.

## Proposed authority

`corridor-peer-data-connection-open-admission-settlement-authority-domain`

## Validation

Documentation only. No runtime, network, gameplay, rendering, test, workflow or deployment changes were made. No browser fixture was executed.