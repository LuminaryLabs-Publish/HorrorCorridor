# HorrorCorridor Turn Ledger

**Timestamp:** `2026-07-12T18-31-01-04-00`  
**Selected repository:** `LuminaryLabs-Publish/HorrorCorridor`  
**Branch:** `main`

## Summary

Selected HorrorCorridor after confirming all nine eligible Publish repositories were tracked, synchronized and contained root `.agent` state. Audited the room/join-code allocation path and documented the missing reservation, host-peer ownership admission, retry generation and visible hosting proof.

## Plan ledger

**Goal:** record one complete documentation run for room identity allocation without changing runtime behavior.

- [x] Full Publish inventory compared with central ledgers.
- [x] `TheCavalryOfRome` excluded.
- [x] HorrorCorridor selected by oldest synchronized timestamp.
- [x] Interaction loop, domains, 29 kits and services preserved.
- [x] Source path inspected in `GameShell.tsx` and `createHost.ts`.
- [x] Required root and timestamped audit files updated.
- [x] Publish changes targeted only `main`.
- [x] No branch or pull request created.
- [ ] Runtime collision, retry and cross-mode identity fixtures remain unavailable.

## Finding

`makeJoinCode()` creates four base-36 characters from `Math.random()`. The value is published in room and UI state before `createHost()` proves PeerJS ownership. The transport creates `new Peer(joinCode)` and a same-code `BroadcastChannel`; generic peer errors do not trigger a new code, a new identity generation, rollback or a typed failed-host result.

## Required authority

```txt
corridor-room-join-code-allocation-authority-domain
```

## Change scope

Documentation only. No runtime source, package, dependency, network, gameplay, render or deploy behavior changed.