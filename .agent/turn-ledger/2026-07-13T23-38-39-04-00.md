# HorrorCorridor Turn Ledger

**Timestamp:** `2026-07-13T23-38-39-04-00`  
**Repository:** `LuminaryLabs-Publish/HorrorCorridor`  
**Branch:** `main`

## Goal

Document the gap between the loading UI's named progress rows and the actual maze/render work required before the first playable visible frame.

## Completed

- Compared the full Publish inventory against the central ledger.
- Excluded `TheCavalryOfRome`.
- Selected HorrorCorridor as the oldest eligible synchronized repository.
- Inspected `GameShell.tsx`, `LoadingScreen.tsx`, `GameCanvas.tsx`, stores, protocol surfaces and package scripts.
- Preserved the complete interaction loop, active domain map, 29 implemented kits and offered services.
- Defined `corridor-loading-progress-readiness-evidence-authority-domain`.
- Added architecture, render, gameplay, interaction, loading, deployment and central-sync audits.
- Refreshed root `.agent` state and machine registry.

## Main finding

`runLoadingSteps()` advances each authored loading row through a frame and a 90 ms timeout. The named subsystems do not execute or return results during those rows. Maze bootstrap occurs afterward, and renderer/world construction begins only after the application has entered `PLAYING` and set rendering ready.

## Validation boundary

Documentation only. No runtime, test, dependency, workflow or deployment behavior changed. No browser fixture was run.