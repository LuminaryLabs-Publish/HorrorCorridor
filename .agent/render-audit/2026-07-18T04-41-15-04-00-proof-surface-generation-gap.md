# Proof-Surface Generation Gap

**Run:** `2026-07-18T04-41-15-04-00`

## Render loop

```txt
natural-domain and gameplay state
  -> renderer-neutral object/material/light/camera descriptors
  -> Three.js host realization
  -> post-processing and Canvas2D/React projection
  -> browser capture
  -> metric and human-view judgment
  -> durable proof artifact
```

## Current evidence

The nested application documentation cites reset/replay, canonical live-player, endless success, caught loss, restart, reconnect, floor material, wet response, ceiling structure/fracture/material, generator silhouette and industrial shelving proofs.

The canonical graph declares six proof surfaces and four hosts. The latest coverage ledger cites a passing live-player report and passing focused proof reports, but those proofs are stored across multiple dated and `latest/` paths.

## Gap

Proof artifacts do not yet expose one mandatory generation identity joining:

```txt
canonical domain source hash
blueprint generation
runtime install-order digest
behavior coverage generation
render descriptor generation
browser artifact generation
human judgment generation
```

A file path named `latest` is convenient, but it is not a durable authority by itself. Root `.agent` previously did not distinguish current admitted proof evidence from historical, comparison-only or rejected screenshot attempts.

## Required proof admission

```txt
ProofEvidenceAdmissionCommand
  -> proof id
  -> source commit
  -> canonical source hash
  -> blueprint hash
  -> runtime install digest
  -> profile and browser origin
  -> captured-frame digest
  -> automated gate result
  -> human-review result
  -> ProofEvidenceAdmissionResult
```

## Required states

```txt
accepted
rejected
comparison-only
superseded
incomplete
stale-generation
```

## Completion gate

Do not claim full render parity or architecture-proof convergence until each accepted frame cites the same canonical domain and runtime generation represented by the root `.agent` and central ledger.

No renderer, material, geometry or proof script was changed by this audit.