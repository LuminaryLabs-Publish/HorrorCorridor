# HorrorCorridor Current Audit

**Repository:** `LuminaryLabs-Publish/HorrorCorridor`  
**Branch:** `main`  
**Updated:** `2026-07-18T04-41-15-04-00`  
**Status:** `natural-domain-execution-root-agent-authority-reconciled`

## Summary

HorrorCorridor was 12 commits ahead of its documented repo-local head. The delta establishes a substantially deeper nested product architecture and proof corpus under `HorrorCorridor-V1/`.

The current executable census is:

```txt
474 total installs
36 core kits
359 natural-domain owner kits
73 local descriptor kits
6 composition kits
405 registered paths
```

The canonical product inventory contains 359 natural domains, 59 services, 428 target behavior contracts, four hosts and six proof surfaces. The coverage ledger classifies 132 contracts closed and 296 open.

## Interaction loop

```txt
solo/host/client entry
  -> install runtime composition
  -> begin expedition
  -> stream successive ruined buildings
  -> move, inspect and follow directional signs
  -> encounter, study or repel monsters with the flashlight
  -> collect Monster Index knowledge and score
  -> accept optional room offers
  -> continue, become caught, restart or recover shared play
  -> project through Three.js/Web Audio/React/Canvas2D hosts
  -> validate through deterministic and browser proof surfaces
```

## Domain authority

```txt
n:horror-corridor
  -> expedition
  -> corridor
  -> party
  -> dread
  -> shared-expedition
```

The exhaustive recursive tree is `HorrorCorridor-V1/.agent/horror-corridor-domain-tree.md`. The executable blueprint is `HorrorCorridor-V1/src/engine/generated/horrorCorridorDomainBlueprint.json`.

## Kit authority

- 36 current NexusEngine cores are installed from the pinned public revision.
- 359 generated DSKs own natural-domain state, revision, snapshot, patch and reset.
- 73 local descriptor kits own reusable maze, chamber, material, lighting, water, vegetation, placement and ruin-object descriptors.
- Six composition kits assemble the five top-level branches and whole game.
- 428 target behavior-kit contracts remain independently classified in `HorrorCorridor-V1/.agent/domain-service-coverage.md`.

## Main finding

The root `.agent` registry previously described only an older 29-kit application-shell abstraction plus two proof adapters. That inventory remains historically useful, but it is not the current exhaustive runtime or product-architecture census.

The root and nested `.agent` folders need one explicit generation contract joining canonical source, generated blueprint, runtime census, coverage classification, proof evidence and central settlement.

## Proposed authority

```txt
horror-corridor-domain-documentation-generation-authority-domain
```

## Required results

```txt
CanonicalDomainSourceAdmissionResult
DomainBlueprintGenerationResult
RuntimeCompositionCensusResult
BehaviorCoverageClassificationResult
ProofEvidenceAdmissionResult
RootAgentProjectionCommitResult
CentralLedgerCommitResult
```

## Retained gaps

The PeerJS connection-open admission issue and all earlier networking, lifecycle, input, render, proof and deployment findings remain open. The current run does not supersede or resolve them.

## Claim boundary

Documentation only. Existing nested checks and proof reports were inspected but not rerun. No runtime, gameplay, networking, render, deployment or production-readiness claim is added.